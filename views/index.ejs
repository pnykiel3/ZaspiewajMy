<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ZaśpiewajMy - Strona Główna</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body class="bg-dark text-light">

    <section class="hero-section d-flex align-items-center justify-content-center text-center" style="min-height: 50vh;">
        <div class="container">
            <h1 class="display-1 fw-bold mb-4">🎤 Zaśpiewaj<span class="text-primary">My</span></h1>
            <p class="lead mb-4">Znajdź swój ulubiony utwór i zacznij śpiewać!</p>
            
            <div class="row justify-content-center">
                <div class="col-md-8 col-lg-6">
                    <form action="/search" method="GET" class="d-flex shadow-lg">
                        <input type="text" name="q" class="form-control form-control-lg rounded-start" placeholder="Wpisz tytuł lub artystę..." aria-label="Szukaj">
                        <button class="btn btn-primary btn-lg rounded-end px-4" type="submit">
                            <i class="bi bi-search"></i> Szukaj
                        </button>
                    </form>
                </div>
            </div>
            <div class="mt-5">
                <a href="/add-song" class="btn btn-primary">Dodaj nowy utwór</a>
            </div>
        </div>
    </section>

    <section class="container my-5">
        <h2 class="text-center mb-4">Wybierz Gatunek</h2>
        
        <%- include('genres') %>
        
    </section>

    <section class="container my-5">
        <h2 class="mb-4 border-bottom pb-2 border-secondary">🔥 Lista Przebojów (Top 10)</h2>
        
        <div class="list-group">
            <% if (trends && trends.length > 0) { %>
                <% trends.forEach(function(song, index) { %>
                    <a href="/song/<%= song.id %>" class="list-group-item list-group-item-action bg-dark text-light border-secondary d-flex justify-content-between align-items-center mb-1 rounded">
                        <div>
                            <span class="badge bg-danger rounded-pill me-3"><%= index + 1 %></span>
                            <span class="fw-bold fs-5"><%= song.title %></span>
                            <span class="text-muted ms-2">- <%= song.artist %></span>
                        </div>
                        <span class="badge bg-primary rounded-pill">
                            👁️ <%= song.views %> wyświetleń
                        </span>
                    </a>
                <% }); %>
            <% } else { %>
                <div class="alert alert-info">Brak utworów w bazie danych.</div>
            <% } %>
        </div>
    </section>

    <footer>
        <p>2025 Paweł Nykiel</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>