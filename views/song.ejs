<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ZaśpiewajMy - <%= song.title %></title> <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-light">

    <header class="main-header">
        <div class="container">
            <h1><a href="/">🎤 Zaśpiewaj<span class="text-primary">My</span></a></h1>
            <nav>
                <a href="/">Szukaj</a> | <a href="/add-song">Dodaj utwór</a>
            </nav>
            <div class="container mt-3">
                <% if (locals.msg === 'duplicate') { %>
                    <div class="alert alert-warning alert-dismissible fade show" role="alert">
                        <strong><i class="bi bi-exclamation-triangle-fill"></i> Uwaga!</strong> Ta piosenka już istnieje w naszej bazie. Przeniesiono Cię do niej automatycznie.
                    </div>
                <% } %>

                <% if (locals.msg === 'success') { %>
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <strong><i class="bi bi-check-circle-fill"></i> Sukces!</strong> Piosenka została dodana poprawnie.
                    </div>
                <% } %>
            </div>
        </div>
    </header>

    <main class="container mt-4">
        
        <article class="song-view">
            <header class="song-header">
                <h2><%= song.title %></h2>
                <p class="artist">Wykonawca: <strong><%= song.artist %></strong></p>
                <p class="views">Wyświetleń: <%= song.views %></p>
            </header>

            <section class="mt-4 mb-4" style="max-width: 800px; margin: 0 auto;">
                <div class="ratio ratio-16x9 shadow rounded">
                    <iframe src="<%= song.youtube_url %>" allowfullscreen></iframe>
                </div>
            </section>

            <section class="lyrics-box">
                <h3 class="text-primary">Tekst piosenki:</h3>
                <pre><%= song.lyrics %></pre>
            </section>
        </article>

        <hr>

        <section class="comments-section">
            <h3 class="text-primary">Komentarze</h3>
            
            <form action="/add-comment" method="POST" class="mb-4">
                <input type="hidden" name="song_id" value="<%= song.id %>">
                <div class="mb-3">
                    <label>Twój nick:</label>
                    <input type="text" name="nick" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label>Komentarz:</label>
                    <textarea name="comment" class="form-control" rows="3" required></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Wyślij</button>
            </form>

            <div class="reviews-list">
                <% comments.forEach(function(comment) { 
                    const date = new Date(comment.created_at);
                    const day = String(date.getDate())
                    const month = String(date.getMonth() + 1)
                    const year = date.getFullYear();
                    const hour = String(date.getHours())
                    const minute = String(date.getMinutes())
                    const formattedDate = `${day}.${month}.${year} o ${hour}:${minute}`;
                %>
                    
                    <div class="comment-card card bg-dark border-secondary mb-3 p-3">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <span class="text-primary fw-bold">
                                <i class="bi bi-person-circle"></i> <%= comment.user_name %>
                            </span>
                            <small class="text-white" style="font-size: 0.85em;">
                                <%= formattedDate %>
                            </small>
                        </div>
                        
                        <p class="mb-0 text-light"><%= comment.comment %></p>
                    </div>

                <% }); %>
            </div>
        </section>

    </main>

    <footer>
        <p>2025 Paweł Nykiel</p>
    </footer>

</body>
</html>